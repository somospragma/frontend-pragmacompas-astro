---
import MentorshipTable from "@/components/organisms/MentorShipTable/MentorShipTable";
import DashboardLayout from "@/components/templates/DashboardLayout.astro";
import { getTutoringRequests } from "@/infrastructure/services/getTutoringRequests";

const handleGetTutoringRequests = () => {
  getTutoringRequests({}).then((data) => {
    console.log(data);
    return data.data;
  });

  return [];
};
---

<DashboardLayout>
  <div class="space-y-6">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
      <div class="bg-card border border-border rounded-lg p-4">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-2xl font-bold text-foreground">5</p>
            <p class="text-muted-foreground text-sm">Total Solicitudes</p>
          </div>
          <div class="w-12 h-12 bg-primary/10 rounded-lg flex items-center justify-center">
            <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
              ></path>
            </svg>
          </div>
        </div>
      </div>

      <div class="bg-card border border-border rounded-lg p-4">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-2xl font-bold text-foreground">2</p>
            <p class="text-muted-foreground text-sm">Pendientes</p>
          </div>
          <div class="w-12 h-12 bg-yellow-500/10 rounded-lg flex items-center justify-center">
            <svg class="w-6 h-6 text-yellow-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
        </div>
      </div>

      <div class="bg-card border border-border rounded-lg p-4">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-2xl font-bold text-foreground">1</p>
            <p class="text-muted-foreground text-sm">Aprobadas</p>
          </div>
          <div class="w-12 h-12 bg-green-500/10 rounded-lg flex items-center justify-center">
            <svg class="w-6 h-6 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
          </div>
        </div>
      </div>
    </div>

    <MentorshipTable title="Solicitudes de Mentoría" client:load mentorshipRequests={handleGetTutoringRequests()} />
  </div>
</DashboardLayout>
